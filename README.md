# Application Library

The library contains all applications profiles for CGMES and NC Profiles versions.

Note that for some of the previous versions contain a folder "Enchanced". the content of this forder is new version of the machine-readable artifacts generated on the same version of the profile. This happens in cases where tooling was updated in the meantine. 
In case required enchanced versions can be generated for other previous versions as well.
